import { useCollection } from "../../hooks/useCollection";


const BasicInfo = () => { 
    // const { ducuments:basicInfo } = useCollection('basicInfo') 
    // const { ducuments:dailyPE } = useCollection('dailyPE') 

    return ( <div>basicInfo</div> );
}
 
export default BasicInfo;